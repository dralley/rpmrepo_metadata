<?xml version="1.0" encoding="UTF-8"?>
<!--
  RELAX NG XML schema for updateinfo.xml — machine-readable equivalent of
  updateinfo.rnc. Use with: xmllint - -relaxng updateinfo.rng somefile.xml - -noout
  The .rnc file is the human-readable primary; this .rng is generated for
  tooling compatibility (xmllint does not accept compact syntax).
-->
<grammar xmlns="http://relaxng.org/ns/structure/1.0"
         datatypeLibrary="http://www.w3.org/2001/XMLSchema-datatypes">

  <start>
    <element name="updates">
      <zeroOrMore><ref name="update"/></zeroOrMore>
    </element>
  </start>

  <!-- A single advisory record -->
  <define name="update">
    <element name="update">
      <!-- The issuing authority, typically an email address
           (e.g. "security@redhat.com", "updates@fedoraproject.org").
           Has been seen missing in the wild (per libzypp). -->
      <attribute name="from"><text/></attribute>
      <!-- Release status. Common values: "final", "stable", "testing",
           "pending", "retracted". -->
      <attribute name="status"><text/></attribute>
      <!-- Advisory type. Red Hat/Fedora values: "security", "bugfix",
           "enhancement", "newpackage". SUSE values: "security",
           "recommended", "optional", "feature". -->
      <attribute name="type"><text/></attribute>
      <!-- Version of the advisory metadata itself (integer string,
           incremented when the advisory is revised). -->
      <attribute name="version"><text/></attribute>

      <!-- Child elements can appear in any order (interleave).
           NOTE: libxml2's xmllint has limitations validating complex interleave
           patterns with many optional elements; validation failures do not
           necessarily indicate schema violations. -->
      <interleave>
        <element name="id"><text/></element>
        <element name="title"><text/></element>
        <optional>
          <!-- Usually a self-closing element with a date attribute, but has been
               seen in the wild with the timestamp as text content instead (per libzypp).
               Common date formats: "YYYY-MM-DD HH:MM:SS", "YYYY-MM-DD HH:MM:SS UTC",
               or a Unix epoch timestamp. -->
          <element name="issued">
            <choice>
              <attribute name="date"><text/></attribute>
              <text/>
            </choice>
          </element>
        </optional>
        <optional>
          <!-- Same format as issued. -->
          <element name="updated">
            <choice>
              <attribute name="date"><text/></attribute>
              <text/>
            </choice>
          </element>
        </optional>
        <optional><element name="rights"><text/></element></optional>
        <optional><element name="release"><text/></element></optional>
        <optional><element name="pushcount"><text/></element></optional>
        <optional><element name="severity"><text/></element></optional>
        <optional><element name="summary"><text/></element></optional>
        <optional><element name="description"><text/></element></optional>
        <optional><element name="solution"><text/></element></optional>
        <optional><element name="message"><text/></element></optional>
        <optional><element name="reboot_suggested"><text/></element></optional>
        <optional>
          <element name="references">
            <zeroOrMore><ref name="reference"/></zeroOrMore>
          </element>
        </optional>
        <optional>
          <element name="pkglist">
            <zeroOrMore><ref name="collection"/></zeroOrMore>
          </element>
        </optional>
      </interleave>
    </element>
  </define>

  <!-- Reference (bugzilla, CVE, errata link) -->
  <define name="reference">
    <element name="reference">
      <!-- URL to the external resource. May be empty or absent in rare cases. -->
      <optional><attribute name="href"><data type="anyURI"/></attribute></optional>
      <!-- Identifier within the external system (bug number, CVE ID, errata ID).
           May be empty or absent for type="self" references and documentation links. -->
      <optional><attribute name="id"><text/></attribute></optional>
      <!-- Reference type. Common values: "bugzilla", "cve", "self", "other", "vendor".
           May be empty or absent in rare cases. -->
      <optional><attribute name="type"><text/></attribute></optional>
      <!-- Human-readable title or description. May be empty or absent,
           especially for type="other" documentation references. -->
      <optional><attribute name="title"><text/></attribute></optional>
    </element>
  </define>

  <!-- Package collection -->
  <define name="collection">
    <element name="collection">
      <!-- Short identifier for the collection (e.g. "rhel-9-baseos", "F40"). -->
      <optional><attribute name="short"><text/></attribute></optional>
      <interleave>
        <optional><element name="name"><text/></element></optional>
        <optional><ref name="module"/></optional>
        <zeroOrMore><ref name="package"/></zeroOrMore>
      </interleave>
    </element>
  </define>

  <!-- Module stream metadata (NSVCA) -->
  <define name="module">
    <element name="module">
      <attribute name="name"><text/></attribute>
      <attribute name="stream"><text/></attribute>
      <attribute name="version"><data type="unsignedLong"/></attribute>
      <attribute name="context"><text/></attribute>
      <attribute name="arch"><text/></attribute>
    </element>
  </define>

  <!-- A package affected by the advisory -->
  <define name="package">
    <element name="package">
      <attribute name="name"><text/></attribute>
      <attribute name="epoch"><text/></attribute>
      <attribute name="version"><text/></attribute>
      <attribute name="release"><text/></attribute>
      <attribute name="arch"><text/></attribute>
      <optional><attribute name="src"><text/></attribute></optional>
      <interleave>
        <optional><element name="filename"><text/></element></optional>
        <optional>
          <!-- Package checksum as element text, with algorithm in the type attribute. -->
          <element name="sum">
            <!-- Checksum algorithm (e.g. "sha256", "sha1", "md5"). -->
            <attribute name="type"><text/></attribute>
            <text/>
          </element>
        </optional>
        <optional><element name="reboot_suggested"><text/></element></optional>
        <optional><element name="restart_suggested"><text/></element></optional>
        <optional><element name="relogin_suggested"><text/></element></optional>
      </interleave>
    </element>
  </define>

</grammar>
